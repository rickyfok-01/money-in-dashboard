# 14 — Trustee × Channel (tab 13)

Cross-analysis: which **submit channels each trustee** relies on. Channels as columns, trustees as rows.

## Drives
snapshot · scheme (multi-select).

## Measures
5 channel counts summed per trustee.

## Charts (top)
1. **Channel mix per trustee** — horizontal stacked bar, categorical 5.
2. **Trustee × Channel matrix** — heatmap (sequential blue).

## Table
`TRUSTEE | DDE | BATCH | PORTAL | BULK | OTHER | Total` (sorted by total).

## Modes
Current · Compare (Δ total per trustee). Trend hidden.

## Notes
- Reveals channel strategy differences between trustees (e.g., one trustee mostly BATCH, another PORTAL).
