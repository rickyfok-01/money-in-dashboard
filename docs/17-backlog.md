# 17 — Backlog & Pending (tab 16)

Operational **work-in-progress**: bills still `OPEN / PARTIAL_SUBMIT / SUBMITTED / APPROVED` (not yet paid).

## Drives
snapshot · scheme (multi-select).

## Measures
`BILL_COUNT` restricted to the 4 pending statuses.

## Charts (top)
1. **Pending bills by scheme** — horizontal bar (top-15 + Other), sequential emphasis.
2. **Backlog composition** — vertical bar by pending status, categorical.
3. **Backlog over months** — line of pending bills per month.

## Table
`SCHEME | OPEN | PARTIAL_SUBMIT | SUBMITTED | APPROVED | Pending` (sorted by pending).

## Modes
Current · Compare.

## Notes
- The "where is the work stuck" tab — ranks schemes by outstanding bill volume.
- Pairs with Completion Rate (terminal side of the same lifecycle).
